node_modules/
dist/
.DS_Store
*.log
.vercel

# Generated at build time by build-edition.mjs (npm run build runs it first),
# so the editions are always as fresh as the deploy. Nothing generated is
# tracked — run `npm run editions` once before `npm run dev` locally.
public/editions/
